name: flutter_tp_corrige
version: 1.0.0+1
publish_to: 'none'
description: TP Flutter - reproduction et amélioration des applications du support.
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
